// str = cat Makefile|grep -i"SRC"|wc -c>$VAR

// | > < $ " ' && || ( ) + isspace()

// " this is a '$VAR' string " => 

/*

// linked list (next/prev) => LEXER

cat		=> WORD
Makefile => WORD
|		=> PIPE
grep	=> WORD
-i		=> WORD
"SRC"	=> DOUBLE QUOTES
|		=> 	PIPE
wc		=> WORD
-c		=> WORD
>		=> REDIR OUT
$VAR	=> VAR

// EXPANSER

cat
Makefile
|
grep
-i
SRC
|
wc
-c
>
variable
VAR
value


// PARSER

Cmd1 => cat Makefile

Cmd2 => grep -i SRC

Cm3 => wc -c

*/